# ===================================
#  List Column Definitions
# ===================================

columns:
  author:
    label: Author
    relation: author
    select: concat(first_name, ' ', last_name)
    searchable: true
  id:
    label: ID
    searchable: true